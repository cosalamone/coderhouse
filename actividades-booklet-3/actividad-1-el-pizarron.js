/* 
Solicitar al usuario un (1) un número y un (1) texto. Efectuar una salida por 
alerta con el mensaje ingresado por cada repetición, hasta alcanzar el valor 
señalado por el usuario.
*/

let numero = prompt("Por favor indica un número");
let texto = prompt("Por favor indica un texto");

for (let i = 0; i < numero; i++) {
    alert(texto)
}